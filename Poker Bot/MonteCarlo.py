from random import choice
from cards import Dealer
from hands import Poker

class MonteCarlo:

    def __init__(self, hand):
        self.hand = hand

    def deal_simulation():
        for cards in range(10):
            lst = []
            lst.append(self.hand)
            lst.append(choice[i for i in range(52) if i not in self.hand]
        return()
